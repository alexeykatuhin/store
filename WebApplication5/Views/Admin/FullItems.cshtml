@model WebApplication5.Models.FullItemViewModel

@{
    ViewBag.Title = "FullItems";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>FullItems</h2>

<p>
    @Html.ActionLink("Create New", "CreateFullItem", new {Id = Model.Id})
</p>
<table class="table">
    <tr>
        <th>
			ItemId
        </th>
        <th>
			Size
        </th>
        <th>
			Quantity
        </th>
        <th></th>
    </tr>
	@{
		var lst = Model.FullItems;
		if (lst.Any())
		{

			foreach (var item in lst)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.ItemId)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Size)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Quantity)
					</td>
					<td>
						@Html.ActionLink("Edit", "EditFullItem", new {id = item.Id}) |
						@Html.ActionLink("Delete", "DeleteFullItem", new {id = item.Id})
					</td>
				</tr>
			}
		}

	}
</table>
<div>
	@Html.ActionLink("Back to List", "Index")
</div>
